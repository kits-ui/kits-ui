import { createApp } from 'vue';
import App from './app.vue';
// import uselessUI from '../dist/index.es.js';
// import '../dist/entry.css';

const app = createApp(App);
// app.use(uselessUI);
app.mount('#app');
